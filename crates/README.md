# Rust core

This directory will contain the Rust libraries shared by FileFlow clients.

Initial responsibility:

- transfer and resumability
- folder synchronization
- conflict detection
- device identity and security-sensitive operations
- platform-independent domain logic

The Rust workspace scaffold and Flutter FFI bindings will be added when the team begins implementation.
